import { radius, stroke } from './border'
import { border, colors, icon, surface, text } from './colors'
import { padding, spacing } from './spacing'
import { fontFamily, fontSize, lineHeight, weight } from './typography'

export const theme = {
  border,
  colors,
  fontFamily,
  fontSize,
  icon,
  lineHeight,
  padding,
  radius,
  spacing,
  stroke,
  surface,
  text,
  weight,
} as const
